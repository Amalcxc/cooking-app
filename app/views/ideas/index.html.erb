<div class="container-fluid p-0 m-0">
 
   <div class="row">
        <div class="col-2">
            <%= render 'menu'%>
        </div> 
        <div class="mt-0 col-10 p-0">
            <%= render 'nav'%> 
              <div class="d-flex"> 

                <%= render 'feed'%> 
                <%= render 'follow'%>
              </div>  
         </div>
    </div>   
</div>
